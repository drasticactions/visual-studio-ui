using Microsoft.StandardUI;
using Microsoft.StandardUI.Controls;

namespace Microsoft.VisualStudioUI
{
    public class ModernDialogImplementation : StandardControlImplementation<IModernDialog>
    {
        public ModernDialogImplementation(IModernDialog control) : base(control)
        {
        }
    }
}
